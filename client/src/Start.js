import React from "react";
import Cards from './Cards';

const Start = ( {onStartGame} ) =>{
    return (
        <div>
            <h2>Welcome to the High or Low Card Game!</h2>
        </div>
    );
}

export default Start;